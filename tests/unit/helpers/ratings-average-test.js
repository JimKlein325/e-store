import { ratingsAverage } from 'e-store/helpers/ratings-average';
import { module, test } from 'qunit';

module('Unit | Helper | ratings average');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ratingsAverage([42]);
  assert.ok(result);
});
